import * as Icons from 'lucide-react';

// All Lucide icons exported as an object
export const lucideIcons = Icons;

// Optional: list icon names only
export const lucideIconNames = Object.keys(Icons);
